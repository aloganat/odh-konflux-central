config:
  default-branches-allowlist:
    - main
    - master
  repositories:
    kserve:
      early-gate-enabled: true
      # additional-branches: []
    #  early-gate-required-on-default-branches: false
  kserve:
    early-gate-enabled: true
